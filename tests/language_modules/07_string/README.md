# 07_string

String is a built-in scalar type with literals, equality checks, and built-in functions under the `String.*` namespace.

This section sits before routines so the routine module can rely on `function returns String` as already covered language surface.

Initial diagnostics:

```text
VF-S001 wrong String argument count
VF-S002 String argument type mismatch
VF-S999 semantic fallback for broader expression errors
```
